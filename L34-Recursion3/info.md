// Calculate Power a^b

// now b can be even or odd

images/PowerQuestionHint.png

2^1024 -> (2^512)^2 -> ......

by recursion, it requires 11 iteration to calculate 2^1024 , but if for loop used , it would require 1024 iterations, so very much optimized

Power question exaple:
recurion tree for 3^11 or isPoer(3,11)
images/PowerEg1.png

